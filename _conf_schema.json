{
  "enable_injection": {
    "description": "注入情感上下文",
    "type": "bool",
    "default": true,
    "hint": "是否在每次 LLM 请求时自动注入你此刻的情感上下文。"
  },
  "analysis_provider_id": {
    "description": "分析模型",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "用于关系分析的 LLM 提供商。必须配置，否则无法执行分析。"
  },
  "analysis_secondary_provider_id": {
    "description": "备用分析模型",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "主模型失败时自动尝试。可留空。"
  },
  "analysis_timeout_seconds": {
    "description": "分析超时（秒）",
    "type": "int",
    "default": 60,
    "hint": "单次 LLM 调用的最大等待时间。"
  },
  "analysis_interval_minutes": {
    "description": "分析间隔（分钟）",
    "type": "int",
    "default": 30,
    "hint": "两次分析之间的最小时间间隔。"
  },
  "min_new_messages": {
    "description": "最少消息数",
    "type": "int",
    "default": 15,
    "hint": "新增消息数达到此值才可能触发分析。"
  },
  "buffer_max_size": {
    "description": "消息缓存上限",
    "type": "int",
    "default": 100,
    "hint": "每个会话在内存中缓存的最大消息条数。"
  },
  "debug_mode": {
    "description": "调试模式",
    "type": "bool",
    "default": false,
    "hint": "开启后注入的提示词内容会输出到日志，排查问题时再开。"
  },
  "enable_live_perception": {
    "description": "对话模型自主感知",
    "type": "bool",
    "default": false,
    "hint": "需要 AstrBot v4.24.2+。开启后不再使用 DB 中可能过时的 user_state 和 tone_hint，改由对话 LLM 实时感知。"
  },
  "enable_live_perception_update": {
    "description": "允许自主修正注入",
    "type": "bool",
    "default": false,
    "hint": "需要先开启「对话模型自主感知」。允许 LLM 在发现感知过时时通过 <update> 标签修正。"
  },
  "enable_dialogue_static_update": {
    "description": "对话模型驱动静态注入",
    "type": "bool",
    "default": false,
    "hint": "需要先开启「对话模型自主感知」。开启后所有静态注入内容（氛围、语气、对方状态）始终由对话模型的 <rs> 标签驱动，后台模型只负责五维数值更新。关闭时仅初始化阶段由对话模型驱动，后续由后台模型接管。"
  },
  "enable_group_mode": {
    "description": "群聊模式",
    "type": "bool",
    "default": false,
    "hint": "开启后在群聊中启用多用户关系感知，私聊不受影响。"
  },
  "unify_cross_session": {
    "description": "跨会话统一关系",
    "type": "bool",
    "default": false,
    "hint": "开启后同一用户在所有会话（私聊+群聊）共享一套五维数据。默认关闭，每个会话独立。"
  },
  "group_active_days": {
    "description": "群聊活跃天数",
    "type": "int",
    "default": 3,
    "hint": "群聊中仅对最近 N 天内说过话的用户维护关系数据。"
  },
  "group_analysis_interval_minutes": {
    "description": "群聊批量分析间隔（分钟）",
    "type": "int",
    "default": 120,
    "hint": "群聊批量补分析的最小间隔。"
  },
  "group_max_active_users": {
    "description": "群聊最大追踪用户数",
    "type": "int",
    "default": 20,
    "hint": "每个群最多追踪多少个用户的关系，超过则按活跃度淘汰。"
  }
}
