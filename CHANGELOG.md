### v1.4.0

**👥 群聊模式：1vN 关系感知**

* 新增 `enable_group_mode` 全局开关，开启后群聊场景自动切换到多用户关系感知逻辑，私聊行为完全不变
* 新增 `unify_cross_session` 可选开关，开启后同一用户在所有会话共享五维数据
* 新增 `group_active_days`、`group_analysis_interval_minutes`、`group_max_active_users` 群聊配置项
* 群聊中按 `platform_groupId_userId` 格式为每个用户维护独立的关系状态
* 新增群聊专用分析提示词（GROUP_ANALYZER_SYSTEM_PROMPT / GROUP_ANALYZER_USER_PROMPT），针对群聊场景调整五维评估规则
* 新增群聊批量分析提示词（GROUP_BATCH_ANALYZER），单次 LLM 调用分析多用户
* 新增群聊专用注入模板（GROUP_INJECTION_*），包含当前说话者上下文 + 活跃用户摘要
* 新增 `group_user_activity` 数据库表，追踪群内用户活跃度和分析时间
* 消息缓存扩展：`add_message` 新增 `sender_id`/`sender_name`/`is_at_bot` 参数
* 新增 `format_group_dialogue` 方法，将缓存消息格式化为带发送者名称的对话文本
* 优先复用 group_chat_plus 的消息缓存，不可用时回退自有缓存
* 异步后台批量补分析循环，定期检查过期活跃用户并批量更新

### v1.3.0

**🏗️ 三层架构重构：防注入累积 + 实时感知 + 自主修正**

**Layer 1 — 基础修复（默认开启，全版本兼容）**
* 每次注入前先删除旧的 RS_Injection 块，用 `<!-- RS_Injection -->` / `<!-- /RS_Injection -->` 包裹注入内容，解决 system_prompt 注入累积

**Layer 2 — 对话 LLM 实时感知（开关，默认关闭，需 v4.24.2+）**
* 新增 `enable_live_perception` 配置项
* 开启后不再从 DB 读取 `user_state` / `tone_hint`，改由对话 LLM 通过 `_no_save` 上下文消息实时感知对方状态
* 五维数值仍由异步分析 LLM 后台更新

**Layer 3 — LLM 自主修正注入（开关，默认关闭，需 v4.24.2+）**
* 新增 `enable_live_perception_update` 配置项
* 允许 LLM 在发现感知过时时通过 `<update>` 标签修正（如早上"刚醒"到晚上还在则自动修正）
* 插件自动解析 `<update>` 标签，更新内存缓存并从回复中移除标签
* `metadata.yaml` 新增 `astrbot_version` 字段

**⚡ 首次印象评估模式 + 手动分析通知**

* 移除冗余的回溯初始化自动触发逻辑，初始关系回归 Lv0「冷淡」
* 新增首次印象评估模式：新会话首次分析时跳过维度惯性上限，LLM 输出直接映射到五维
* 首次分析使用专用提示词，更侧重人设导向的第一印象，而非渐进式调整
* 手动分析不再静默：结果直接发送到对话，包括成功摘要和失败原因

### v1.2.1

**🐛 修复初始化等级异常**

* 修复新会话首次分析时冷启动默认值导致关系等级为 Lv0 的问题（现已被 v1.2.2 的首次印象模式取代）
* 改进回溯初始化提示词：结合人设推断初始情感值
* 重置关系时自动传入平台/用户信息，确保回溯分析能拉取到历史消息

### v1.2.0

**🌐 中文化 + 提示词重构**

* 指令全面中文化：中文名作为主命令（关系状态/关系历史/关系分析/解冻关系/重置关系），英文名保留为别名
* 提示词全面「去 Bot 化」：所有注入和分析提示词中的 "Bot" 统一替换为 "你"，更自然流畅
* 配置面板描述去 "Bot" 化

**🔧 调试增强 + 手动分析 + 配置精简**

* 调试模式升级：开启后在日志中输出完整注入提示词内容
* 新增 `relation_analyze` 指令：手动触发当前会话的关系分析
* 配置面板移除 `enable_plugin`（冗余开关）

### v1.1.0

**🔄 核心升级：人格感知 + 维度惯性 + 注入策略分层 + 冷却**

* 人格注入：常规分析时自动将 Bot 人设传入分析 LLM，情绪评估更贴合角色性格
* 维度惯性：限制单轮最大变化幅度（好感≤12/信任≤8/依赖≤10 等），防止 LLM 情绪波动过大
* 注入策略分 5 层：冲突模式 / 暧昧模式 / 回归模式 / 沉寂模式 / 极简模式，不同场景自动切换
* 对话冷却：长时间无活跃时对话深度与依赖度自然衰减，Bot 不会永远等一个不回的人
* 疏远规则写入分析提示词：敷衍对话时 LLM 主动降对话深度与依赖度

### v1.0.0

**💞 首次发布：Bot 自己的情感系统上线**

* 五维情感指标体系 + 九级全谱系情感等级
* 异步分析引擎 + system_prompt 注入 + 冻结机制 + 回溯初始化
* 管理命令：relation_status / relation_history / relation_unfreeze / relation_reset
